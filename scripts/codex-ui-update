#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "codex-ui-update: deprecated; use codex-lab-install" >&2
exec "$SCRIPT_DIR/codex-lab-install" "$@"
