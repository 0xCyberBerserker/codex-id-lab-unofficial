#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
ENABLE=0
DESKTOP_SHORTCUT=0

usage() {
  cat <<'EOF'
Usage: install-user-companion [--enable] [--desktop-shortcut]

Options:
  --enable            Enable and start the companion user service.
  --desktop-shortcut  Add a trusted launcher to the desktop directory.
  --help              Show this help.
EOF
}

while [[ $# -gt 0 ]]; do
  case "$1" in
    --enable) ENABLE=1 ;;
    --desktop-shortcut) DESKTOP_SHORTCUT=1 ;;
    --help|-h) usage; exit 0 ;;
    *) echo "install-user-companion: unknown option: $1" >&2; usage >&2; exit 2 ;;
  esac
  shift
done

command -v python3 >/dev/null 2>&1 || { echo "install-user-companion: python3 is required" >&2; exit 2; }
python3 -c 'import PySide6' >/dev/null 2>&1 || { echo "install-user-companion: PySide6 is required" >&2; exit 2; }

data_home="${XDG_DATA_HOME:-$HOME/.local/share}"
config_home="${XDG_CONFIG_HOME:-$HOME/.config}"
bin_dir="$HOME/.local/bin"
app_dir="$data_home/codex-ui-linux-port"
applications_dir="$data_home/applications"
icons_dir="$data_home/icons/hicolor/scalable/apps"
unit_dir="$config_home/systemd/user"

install -d -m 755 "$bin_dir" "$app_dir" "$applications_dir" "$icons_dir" "$unit_dir"
install -m 755 "$ROOT/scripts/codex-ui-companion.py" "$app_dir/codex-ui-companion.py"
install -m 644 "$ROOT/resources/codex-ui-tools.desktop" "$applications_dir/codex-ui-tools.desktop"
install -m 644 "$ROOT/docs/assets/codex-ui-linux-port.svg" "$icons_dir/codex-ui-linux.svg"

launcher="$bin_dir/codex-ui-tools"
printf '#!/usr/bin/env bash\nexec /usr/bin/python3 %q "$@"\n' "$app_dir/codex-ui-companion.py" > "$launcher"
chmod 755 "$launcher"

sed "s|^ExecStart=.*|ExecStart=$launcher tray|" \
  "$ROOT/resources/codex-ui-companion.service" > "$unit_dir/codex-ui-companion.service"
chmod 644 "$unit_dir/codex-ui-companion.service"

if command -v update-desktop-database >/dev/null 2>&1; then
  update-desktop-database "$applications_dir" >/dev/null 2>&1 || true
fi
if command -v kbuildsycoca6 >/dev/null 2>&1; then
  kbuildsycoca6 >/dev/null 2>&1 || true
fi

if [[ "$DESKTOP_SHORTCUT" -eq 1 ]]; then
  if command -v xdg-user-dir >/dev/null 2>&1; then
    desktop_dir="$(xdg-user-dir DESKTOP)"
  else
    desktop_dir="$HOME/Desktop"
  fi
  install -d -m 755 "$desktop_dir"
  install -m 755 "$ROOT/resources/codex-ui-tools.desktop" "$desktop_dir/Codex UI Tools.desktop"
fi

if [[ "$ENABLE" -eq 1 ]]; then
  systemctl --user daemon-reload
  systemctl --user enable --now codex-ui-companion.service
fi

echo "install-user-companion: installed $launcher"
