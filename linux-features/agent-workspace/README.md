# agent-workspace — experimental preflight

Main bridge contract matches. Audited helper absent; upstream global npm installation, startup skill writes and sandbox-disabling paths are excluded.

Reference source/commit/license are in `feature.json`; no executable hook is imported. Disabled profiles stage no resources, permissions or activity. Enabling fails before mutation.

## Español

Encaja el puente principal. Falta el helper auditado; se excluyen npm global, escritura de skills al arrancar y rutas que deshabilitan sandbox.

La referencia está fijada en `feature.json`; no se importa ningún hook ejecutable. Deshabilitado no añade recursos, permisos ni actividad. Activarlo falla antes de mutar.
