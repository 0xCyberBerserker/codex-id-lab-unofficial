# remote-mobile-control — experimental preflight

Two main-bundle contracts match. Account entitlement, keychain-backed device keys, consent, pairing and revocation are NOT_RUN. No network listener or remote-control flags are installed.

Reference source/commit/license are in `feature.json`; no executable hook is imported. Disabled profiles stage no resources, permissions or activity. Enabling fails before mutation.

## Español

Encajan dos contratos del bundle. Privilegio de cuenta, keychain, consentimiento, emparejamiento y revocación siguen NOT_RUN. No se instala exposición de red ni flags de control remoto.

La referencia está fijada en `feature.json`; no se importa ningún hook ejecutable. Deshabilitado no añade recursos, permisos ni actividad. Activarlo falla antes de mutar.
