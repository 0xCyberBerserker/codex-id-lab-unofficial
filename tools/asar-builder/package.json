{
  "name": "codex-lab-isolated-asar-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Pinned development-only ASAR parser; never installed into the base runtime.",
  "dependencies": {
    "@electron/asar": "4.3.0"
  }
}
